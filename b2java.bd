
[general]
indent=true
starting=2

[header]
content=<<EOT
package b2java;

/* Java code generated by b2java from b2any toolkit.       */
/* Header code - Copyright (C) 2019 by Kamila Szewczyk  */
/* The original brianfuck code belongs to it's author.     */
/* Have fun!                        ~~ Palaiologos/MENACE  */

public class Generated {
    private int[] memory = new int[65536];
    private int mp = 0;

    private int get() { return System.in.available() > 0 ? System.in.read() : 0; }
    private void put(int c) { return System.out.print((char) c); }
    public static void main(String[] args) {
EOT

[footer]
content=<<EOT
    }
}

EOT

unbal=<<EOT
/* Note: Brackets are unbalanced, this program will NOT compile. */
EOT

[instructions]
plus=memory[mp] += $N;
minus=memory[mp] -= $N;
left=mp -= $N;
right=mp += $N;
input=memory[mp] = get();
output=put(memory[mp]);
lbra=while(memory[mp] != 0) {
rbra=}
